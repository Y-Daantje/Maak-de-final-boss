fetch('gods.json')
.then(myData => myData.Json())
.then(myData1 => showInConsole(JsonData));
console.log(myData1);


function createCard() {
    for (let i = 0; i < array.length; i++) {
        const card = array[i];
        
    }
}